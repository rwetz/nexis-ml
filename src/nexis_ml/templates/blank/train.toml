# A blank starting point — the model lives in train.py (edit the `Net`
# class). This file just holds the training knobs.

[train]
epochs = 100
lr = 0.01
seed = 42
device = "auto"   # auto | cpu | gpu
